import 'package:flutter/material.dart';

class Constants {
  static var kColorScheme = ColorScheme.fromSeed(
    seedColor: const Color(0xFFBEABA1),
  );
  static var kColorDarkScheme = ColorScheme.fromSeed(
    seedColor: const Color(0xFFE4935D),
  );
}
